{
  "name": "Link",
  "category": "atom",
  "status": "stable",
  "purpose": "Navigation.",
  "intent": "Hyperlink behavior.",
  "figmaSource": {
    "file": "Components v1.0.0",
    "fileKey": "M0eINB6n1BfrXu7ntYqb1i",
    "componentSetNodeId": "1612:9862",
    "componentName": "link",
    "variantCount": 96,
    "verifiedAt": "2026-09-11",
    "variantAxes": {
      "state": ["default", "hover"],
      "target": ["_self", "_blank"],
      "appearance": ["default", "subtle", "inverse"],
      "hasVisited": ["false", "true"],
      "size": ["md", "sm"],
      "emphasis": ["strong", "default"]
    },
    "notes": [
      "emphasis is a weight axis: strong = heading weight (600), default = body weight (400). Figma default emphasis is 'strong'.",
      "hasVisited applies to every appearance including inverse; inverse visited is content/accent/purple/subtle in Figma (that token is not yet in the generated set, so code uses content/accent/purple/default as the closest available).",
      "Figma's Link has only default + hover states - there is no press/active treatment.",
      "Default appearance colour is content/brand/primary/default (matches Figma)."
    ]
  },
  "useCases": [
    "Internal navigation",
    "External links"
  ],
  "antiPatterns": [
    "Submitting forms",
    "State-changing actions"
  ],
  "anatomy": [
    "anchor",
    "content",
    "automatic external-link icon"
  ],
  "props": {
    "href": {
      "type": "string",
      "required": true
    },
    "children": {
      "type": "React.ReactNode",
      "required": true
    },
    "appearance": {
      "type": "enum",
      "values": ["default", "subtle", "inverse"],
      "default": "default",
      "required": false
    },
    "size": {
      "type": "enum",
      "values": ["sm", "md"],
      "default": "md",
      "required": false
    },
    "emphasis": {
      "type": "enum",
      "values": ["default", "strong"],
      "default": "strong",
      "required": false,
      "description": "Text weight: strong (heading weight) for a name/first-column identifier; default (body weight) for any other entity link."
    },
    "target": {
      "type": "string",
      "default": "_self",
      "required": false
    },
    "rel": {
      "type": "string",
      "default": null,
      "required": false
    }
  },
  "variants": {
    "appearance": [
      "default",
      "subtle",
      "inverse"
    ],
    "emphasis": [
      "default",
      "strong"
    ]
  },
  "sizes": [
    "sm",
    "md"
  ],
  "states": [
    "default",
    "hover",
    "focus",
    "visited"
  ],
  "slots": [
    "children"
  ],
  "tokens": {},
  "accessibility": {
    "nativeElement": "a",
    "accessibleNameRequired": true
  },
  "keyboardInteraction": [
    "Enter activates"
  ],
  "behaviorRules": [
    "Use anchor semantics.",
    "External links may show icon."
  ],
  "compositionRules": [
    "Use LinkButton when link should appear as button."
  ],
  "contentRules": [
    "Use descriptive link text."
  ],
  "responsiveRules": [],
  "themeRules": [],
  "implementationNotes": [
    "CSS Modules",
    "Shared FocusRing"
  ],
  "relatedComponents": [
    "Button",
    "LinkButton",
    "Tooltip"
  ],
  "do": [
    "Use for navigation"
  ],
  "dont": [
    "Use for actions"
  ],
  "acceptanceCriteria": [
    "Keyboard accessible",
    "Focus visible",
    "Correct semantics"
  ]
}