{
  "name": "ModalDialog",
  "category": "organism",
  "status": "stable",
  "purpose": "Interrupt the flow to focus the user on a single task or decision, over a backdrop, with the modal behaviour handled.",
  "intent": "A controlled overlay: a blanket backdrop centering a panel of header (title + header actions), a scrollable body, and a footer of actions. The consumer owns `open` and reacts to `onClose`. Composes atoms (Icon Button, Buttons, status Icon) into a complete self-contained overlay that portals above the page, traps focus, closes on Escape/backdrop, locks page scroll, and restores focus - which is why it lives at the organism tier.",
  "nativeElement": "div (role=dialog, portaled)",
  "sourceFiles": {
    "component": "modal-dialog.tsx",
    "types": "modal-dialog.types.ts",
    "styles": "modal-dialog.module.css",
    "stories": "ModalDialog.stories.tsx",
    "tests": "ModalDialog.test.tsx",
    "index": "index.ts"
  },
  "figmaSource": {
    "file": "Components v1.0.0",
    "fileKey": "M0eINB6n1BfrXu7ntYqb1i",
    "componentSetNodeId": "2456:12608",
    "componentSetName": "modal-dialog",
    "variantCount": 12,
    "verifiedAt": "2026-09-08",
    "figmaVariantAxes": {
      "appearance": ["default", "warning", "error"],
      "width": ["small", "medium", "large", "extra large"]
    },
    "panelWidths": { "small": 400, "medium": 600, "large": 752, "extraLarge": 968 },
    "subParts": [
      { "name": "blanket", "note": "Backdrop overlay filling the viewport, fill color/background/brand/secondary/overlay/blanket, centering the panel. Figma's 64px around the panel was artboard framing; the code insets the panel with --spacing-4xl." },
      { "name": "modal-header", "componentSetNodeId": "2456:2409", "note": "appearance default/breadcrumb/warning/error. Title (heading/md, content/default) + a header action group: an Expand (grow_diagonal) icon-button and the Close icon-button (both subtle, sm) in a Button Group. warning/error prepend StatusWarningIcon / StatusErrorIcon coloured content/warning / content/error. The 'breadcrumb' appearance swaps the title for breadcrumbs - not built as a dedicated prop; pass breadcrumbs as `title` with an aria-label if needed." },
      { "name": "modal-footer", "componentSetNodeId": "2456:11691", "note": "appearance default/warning/error; a right-aligned action group (Cancel + Confirm). Its left-side filter area is example content, hidden by default - the code footer is a right-aligned slot." }
    ],
    "apiAdaptationNotes": [
      "Built standalone, not on the Popup primitive - Popup is a trigger-anchored positioned popover with no focus trap, backdrop, aria-modal, or scroll lock; a modal is a centered focus-trapped overlay, so createPortal + a focus trap are used directly.",
      "Controlled with open + onClose (not Popup's onOpenChange) - a modal only ever requests close, and onClose lets the consumer intercept it.",
      "Figma's appearance (default/warning/error) prepends a decorative status icon to the title; it does NOT tone the footer buttons (Modal Dialog can't know the action layout), so consumers pair appearance='error'/'warning' with a matching Confirm button tone - the same wrapper-vs-control split Field uses for state.",
      "The footer is a right-aligned slot; Figma's footer left filter area (dropdown/date field) is example content hidden by default, not part of the component.",
      "Surface and backdrop inset use existing tokens by intent: the panel surface is --color-elevation-surface-raised-default (the shared floating-surface token; Figma names the same role overlay/default), and the backdrop fills the viewport, insetting the panel with --spacing-4xl (Figma's 64px around the panel was artboard framing, not a design value).",
      "Panel widths (inner) are 400/600/752/968 for small/medium/large/extraLarge; the panel shrinks below its width on narrow viewports via max-inline-size:100%.",
      "The breadcrumb header appearance is not a dedicated code prop - it is niche; a breadcrumbs header can be achieved by passing a Breadcrumbs element as `title` plus an aria-label."
    ]
  },
  "useCases": [
    "a destructive confirmation (error appearance + a red Confirm button)",
    "a save-changes / discard prompt",
    "a short focused form",
    "an informational dialog that needs acknowledgement"
  ],
  "antiPatterns": [
    "non-blocking transient feedback (use Toast)",
    "contextual overflow anchored to a control (use Dropdown Menu / Menu)",
    "a large or multi-step flow (prefer a full page)"
  ],
  "anatomy": [
    { "part": "backdrop", "element": "div.backdrop", "required": true, "description": "Fixed full-viewport blanket that centers the panel with --spacing-4xl inset; a genuine click on it requests close." },
    { "part": "panel", "element": "div.panel (role=dialog, aria-modal, tabIndex -1)", "required": true, "description": "The dialog surface - raised-surface bg, --border-radius-xxl, --color-border-default. A flex column of header/body/footer; width per the width prop; shrinks on narrow viewports." },
    { "part": "header", "element": "div.header", "required": false, "description": "Rendered when there's a title or any header action. Status icon + title + header actions. Padding --spacing-2xl/--spacing-lg." },
    { "part": "statusIcon", "element": "span.statusIcon (aria-hidden)", "required": false, "description": "Decorative md status icon for warning/error appearance, coloured content/warning or content/error." },
    { "part": "title", "element": "h2.title", "required": false, "description": "--typography-heading-md, --color-content-default; the dialog's accessible name via aria-labelledby." },
    { "part": "headerActions", "element": "ButtonGroup (div.headerActions)", "required": false, "description": "Groups the expand + close buttons when both are shown; a lone button renders on its own." },
    { "part": "expand", "element": "IconButton (subtle, sm)", "required": false, "description": "Optional header expand (maximize) button (GrowDiagonalIcon), shown when onExpand is provided; labelled by expandLabel; sits before close." },
    { "part": "close", "element": "IconButton (subtle, sm)", "required": false, "description": "The close (X) button; shown by default; labelled by closeLabel." },
    { "part": "body", "element": "div.body", "required": true, "description": "The scrollable content slot; --typography-body-md; inset by --spacing-2xl; scrolls when tall while header/footer stay pinned." },
    { "part": "footer", "element": "div.footer", "required": false, "description": "Right-aligned action slot; gap --spacing-sm; padding --spacing-lg/--spacing-2xl. Rendered when footer is provided." }
  ],
  "props": {
    "open": { "type": "boolean", "required": true, "description": "Whether the dialog is shown (controlled)." },
    "onClose": { "type": "() => void", "required": true, "description": "Called on any close request (close button, Escape, backdrop). The consumer sets open to false." },
    "title": { "type": "React.ReactNode", "required": false, "default": null, "description": "Header heading and the dialog's accessible name (aria-labelledby)." },
    "appearance": { "type": "ModalAppearance", "required": false, "default": "default", "enum": ["default", "warning", "error"], "description": "Prepends a status icon to the title (warning/error). Does not tone the footer buttons." },
    "width": { "type": "ModalWidth", "required": false, "default": "medium", "enum": ["small", "medium", "large", "extraLarge"], "description": "Panel width: 400 / 600 / 752 / 968 px." },
    "children": { "type": "React.ReactNode", "required": false, "default": null, "description": "The dialog body; scrolls when taller than the viewport allows." },
    "footer": { "type": "React.ReactNode", "required": false, "default": null, "description": "Right-aligned footer actions (e.g. Cancel + Confirm)." },
    "showCloseButton": { "type": "boolean", "required": false, "default": true, "description": "Show the header close (X) button." },
    "closeLabel": { "type": "string", "required": false, "default": "Close", "description": "Accessible label for the close button." },
    "onExpand": { "type": "() => void", "required": false, "default": null, "description": "Called when the header expand (maximize) button is clicked. Provide it to show an Expand button before Close (grouped in a Button Group when both are shown)." },
    "expandLabel": { "type": "string", "required": false, "default": "Expand", "description": "Accessible label / tooltip for the expand button." },
    "closeOnEscape": { "type": "boolean", "required": false, "default": true, "description": "Close when Escape is pressed." },
    "closeOnBackdropClick": { "type": "boolean", "required": false, "default": true, "description": "Close when the backdrop is clicked." },
    "initialFocusRef": { "type": "React.RefObject<HTMLElement | null>", "required": false, "default": null, "description": "Element to focus on open. Defaults to the panel." },
    "aria-label": { "type": "string", "required": false, "default": null, "description": "Accessible name when there is no title." },
    "className": { "type": "string", "required": false, "default": null, "description": "Composes with the panel's class list." }
  },
  "variants": {
    "appearance": ["default", "warning", "error"],
    "width": ["small", "medium", "large", "extraLarge"]
  },
  "sizes": [],
  "states": ["open", "closed"],
  "slots": [
    { "name": "children", "required": false, "supports": ["arbitrary body content"], "interactive": true },
    { "name": "footer", "required": false, "supports": ["Button actions (Cancel + Confirm)"], "interactive": true }
  ],
  "tokens": {
    "color": {
      "backdrop": "--color-background-brand-secondary-overlay-blanket",
      "panelSurface": "--color-elevation-surface-raised-default",
      "panelBorder": "--color-border-default",
      "title": "--color-content-default",
      "statusWarning": "--color-content-warning",
      "statusError": "--color-content-error"
    },
    "layout": {
      "backdropInset": "--spacing-4xl",
      "panelRadius": "--border-radius-xxl (16px)",
      "panelWidths": "400 / 600 / 752 / 968 px",
      "headerPadding": "--spacing-2xl (top/l/r), --spacing-lg (bottom)",
      "headerGap": "--spacing-lg",
      "titleIconGap": "--spacing-sm",
      "headerActionGap": "--spacing-sm (between expand and close, owned by the header Button Group)",
      "bodyInset": "--spacing-2xl",
      "footerPadding": "--spacing-lg (top), --spacing-2xl (l/r/bottom)",
      "footerActionGap": "--spacing-sm"
    },
    "typography": { "title": "--typography-heading-md-*", "body": "--typography-body-md-*" }
  },
  "accessibility": {
    "interactive": true,
    "focusable": true,
    "role": "dialog",
    "requirements": [
      "role='dialog' + aria-modal='true'; named by title (aria-labelledby) or aria-label - always give it an accessible name.",
      "Focus moves into the panel on open, is trapped on Tab/Shift+Tab, and is restored to the trigger on close.",
      "Escape (closeOnEscape) and a genuine backdrop click (closeOnBackdropClick) request close via onClose.",
      "Page scroll is locked while open.",
      "The close button is labelled by closeLabel (default 'Close').",
      "The status icon is decorative (aria-hidden) - meaning is carried by the title and actions.",
      "Pair appearance='error'/'warning' with a matching Confirm button tone - Modal Dialog does not tone footer buttons."
    ]
  },
  "keyboardInteraction": [
    "Escape closes the dialog (when closeOnEscape).",
    "Tab / Shift+Tab cycle focus within the panel and never leave it.",
    "Focus returns to the element that opened the dialog when it closes."
  ],
  "behaviorRules": [
    "Controlled: render nothing when open is false; portal a role=dialog panel into document.body when open.",
    "Every close path (close button, Escape, backdrop) calls onClose; the dialog never closes itself.",
    "Move focus into the panel on open (initialFocusRef or the panel), trap Tab within it, restore focus on close.",
    "Lock document.body scroll while open; restore it on close.",
    "A backdrop click closes only on a genuine press-and-release on the backdrop, not a drag that ends there.",
    "appearance warning/error prepend a decorative status icon; default shows none.",
    "width sets the panel width; the panel shrinks below it on narrow viewports.",
    "Render the header when there's a title or any header action; render the footer when footer is provided.",
    "Show an Expand button before Close when onExpand is provided; wrap the two in a Button Group when both are shown (a lone button renders on its own).",
    "Support className composition on the panel."
  ],
  "compositionRules": [
    "Give the dialog an accessible name via title or aria-label.",
    "Put the actions in footer (typically Cancel + a Confirm Button) - they render right-aligned.",
    "Pass onExpand to add a header expand/maximize action (grouped with Close in a Button Group).",
    "Pair appearance='error'/'warning' with a matching Confirm button tone.",
    "Intercept onClose (e.g. confirm discarding unsaved changes) before setting open=false when needed."
  ],
  "contentRules": [
    "Title is a short, specific question or noun phrase.",
    "Destructive confirmations state the consequence and use an error appearance + a matching Confirm."
  ],
  "responsiveRules": [
    "The panel shrinks to fit narrow viewports (max-inline-size: 100%); the body scrolls when tall."
  ],
  "themeRules": [
    "Must work in light and dark themes - all colours use semantic tokens."
  ],
  "implementationNotes": [
    "Use React.memo; createPortal into document.body; render null when closed or on the server.",
    "Focus trap via a capture-phase keydown listener; scroll lock via document.body.style.overflow.",
    "Use CSS Modules.",
    "No MUI.",
    "No Tailwind.",
    "No hardcoded colours, spacing, radius, or typography (panel px widths and z-index are not token-gated).",
    "Do not build on the Popup primitive - it is a positioned popover, not a modal."
  ],
  "relatedComponents": ["Button", "Icon Button", "Button Group", "Toast", "Dropdown Menu", "Menu"],
  "do": [
    "Give every dialog an accessible name (title or aria-label).",
    "Use appearance='error' + a red Confirm for destructive confirmations.",
    "Keep dialogs focused - one task or decision.",
    "Let the consumer own open and react to onClose."
  ],
  "dont": [
    "Don't use a modal for non-blocking feedback (use Toast) or a large multi-step flow (use a page).",
    "Don't rely on appearance to colour the Confirm button - set the button's tone yourself.",
    "Don't forget an accessible name when there's no visible title.",
    "Don't build modal behaviour on the Popup primitive."
  ],
  "acceptanceCriteria": [
    "Renders nothing when open is false; portals a role='dialog' aria-modal='true' panel into the body when open.",
    "Named by title (aria-labelledby) or aria-label when there's no title.",
    "Close button, Escape (closeOnEscape), and a backdrop click (closeOnBackdropClick) each call onClose; a click inside the panel does not.",
    "Focus moves into the panel on open, is trapped on Tab, and is restored to the trigger on close.",
    "Page scroll is locked while open and restored on close.",
    "warning/error render a status icon; default renders none.",
    "width sets the panel width; the panel shrinks below it on narrow viewports.",
    "Renders a right-aligned footer when footer is provided; the body scrolls when tall.",
    "Shows an Expand button (grouped with Close in a Button Group) when onExpand is provided; none by default.",
    "Composes className onto the panel; uses only semantic tokens; works in light and dark themes.",
    "Storybook includes Playground, Appearances, Widths, DestructiveConfirmation, ScrollingBody, HeaderExpand, and EdgeCases.",
    "Tests cover open/closed, portal + ARIA, all three close paths, the expand button and its grouping, focus move/trap/restore, scroll lock, appearance icon, width, and className."
  ],
  "knownLimitations": [
    "No enter/exit transition beyond a light fade-in.",
    "The breadcrumb header appearance is not a dedicated prop (pass breadcrumbs as title + aria-label).",
    "Single-modal assumption - nested/stacked modals are not specially handled."
  ],
  "futureEnhancements": [
    "Enter/exit transitions (scale/fade) with reduced-motion support.",
    "A dedicated breadcrumb header mode once a real use case appears."
  ]
}
